import random # import random
def printMatrix(n):
    for i in range(n):
         for j in range(n):
              print(random.randint(0,1),end=" ") # printing random value from 1 and 0
         print()
  
print("Enter n: ")
x=int(input()) # take input from user
printMatrix(x) # call function printMatrix
